import 'package:flutter/material.dart';

class SearchStrips extends StatelessWidget {
  const SearchStrips({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(color: Colors.greenAccent);
  }
}
