import 'package:flutter/material.dart';

class ProfileStrips extends StatelessWidget {
  const ProfileStrips({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(color: Colors.indigo);
  }
}
